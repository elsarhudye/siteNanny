{% extends 'base.html.twig' %}

{% block title %}
	Category
	{{category.name}}
{% endblock %}

{% block body %}
	{#{{dump(categories)}}#}
	<h1>Category
		{{category.name | upper}}</h1>
	<table>
		<thead>
			<tr>
				<th>id</th>
				<th>name</th>
				<th>nannies</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td>{{category.id}}</td>
				<td>{{category.name}}</td>
				<td>
					{% for nanny in category.nannies %}
						{{nanny.name}}
					{% endfor %}
				</td>
			</tr>
		</tbody>
	</table>
{% endblock %}
